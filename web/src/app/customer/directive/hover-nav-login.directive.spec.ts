import { HoverNavLoginDirective } from './hover-nav-login.directive';

describe('HoverNavLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverNavLoginDirective();
    expect(directive).toBeTruthy();
  });
});
